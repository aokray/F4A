# This dictionary defines implicit variables that are unnecessary for users
# to experiment with, but are necessary for speed/efficiency/correctness/etc. of the
# learning method
#  
alg_defaults = {
    'LogisticRegression': {'max_iter':1000}
}